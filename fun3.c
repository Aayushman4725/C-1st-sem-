#include <stdio.h>
int sum(int,int);
int main()
{
    int x,y,s;
    printf("Enter the two numbers:");
    scanf("%d,%d",&x,&y);
    s=sum(x,y);
    printf("The sum is %d",s);
    return 0;

}

int sum(int a,int b)
{
    return ;
}
